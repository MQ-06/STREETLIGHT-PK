// M2 placeholder — Filter Sidebar
export default function FilterSidebar({ filters, setFilter, total, filtered }) {
  return (
    <div style={{ padding: 20, color: '#64748B', fontSize: 13 }}>
      M2 — Filter Sidebar
    </div>
  )
}
